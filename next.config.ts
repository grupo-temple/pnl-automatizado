import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Permite importar SVG y otros assets si se necesitan en el futuro
  // No se necesita configuración especial para Supabase + Vercel
}

export default nextConfig
